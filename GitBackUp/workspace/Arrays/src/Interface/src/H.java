//interface H
public interface H{
	
	/*
	 * 
	 *Please note - If you ONLY declare method in interface A & H both then it is of no use
	 * as it is just a template.
	 	e.g display() method is present in interface A & H both.But is just schema.
	 	
	 But if you want  to define and declare the methods in interface then you have  to
	 * declare with different names.Only then you can access in the MAIN method
	 * 
	 */ 
	//public void display();
	
	 public void show();
	

}
